A more generic lightweight alternative to nodemon.

#### Usage
```bash
npx github:flawiddsouza/watchy node app.js
```
where `node app.js` is the command you want to run every time a `.js` file changes in the current directory
